# 3D 模型在线预览设置指南

## 🎯 解决模型显示为黑色剪影的问题

### 问题原因
模型显示为黑色剪影通常由以下原因造成：
1. **模型没有材质/纹理** - Rhino 导出时未包含材质信息
2. **光照设置不当** - 环境光不足或曝光过低
3. **导出格式问题** - 材质信息在导出过程中丢失

---

## 📋 完整解决方案

### 步骤 1: 在 Rhino 中准备模型

#### 1.1 确保模型有材质
- 在 Rhino 中为你的鞋子模型**分配材质**
- 使用 **Material Editor** 设置颜色、纹理、反射等属性
- 确保每个部件都有正确的材质分配

#### 1.2 导出为 GLB 格式
1. 选择要导出的模型
2. 菜单：`File > Export Selected...`
3. 文件类型选择：**`glTF Binary (*.glb)`**
4. 在导出选项中：
   - ✅ **勾选 "Export Materials"**（导出材质）
   - ✅ **勾选 "Export Textures"**（导出纹理）
   - ✅ **勾选 "Export Vertex Colors"**（导出顶点颜色）
   - 设置 **Scale** 为合适的值（通常保持默认）

#### 1.3 保存文件
- 文件名：`model.glb`
- 保存位置：`Shoes/2/model.glb`

---

### 步骤 2: 如果模型仍然显示为黑色

#### 方案 A: 在 Rhino 中重新分配材质
1. 打开 **Material Editor** (`Tools > Material`)
2. 为每个部件创建新材质：
   - **鞋底**: 深灰色/黑色材质
   - **鞋面**: 根据设计设置颜色（灰色/黄色/黑色）
   - **细节部件**: 设置相应的颜色
3. 重新导出

#### 方案 B: 使用 Blender 修复（如果 Rhino 导出有问题）
1. 在 Blender 中导入 `.3dm` 或 `.obj` 文件
2. 为模型分配材质
3. 导出为 `.glb` 格式（确保勾选所有材质选项）

#### 方案 C: 调整 model-viewer 设置
如果模型文件本身没有材质，可以在代码中调整光照：

```html
<model-viewer 
    exposure="2.0"           <!-- 增加曝光度 -->
    shadow-intensity="2.0"    <!-- 增加阴影强度 -->
    environment-image="neutral"  <!-- 使用中性环境光 -->
    tone-mapping="auto"
    ...>
</model-viewer>
```

---

### 步骤 3: 验证模型文件

#### 检查清单
- [ ] 文件大小合理（通常几 MB 到几十 MB）
- [ ] 文件格式为 `.glb`（不是 `.gltf`）
- [ ] 文件路径正确：`Shoes/2/model.glb`
- [ ] 在浏览器中打开时没有控制台错误

#### 测试工具
可以使用以下在线工具预览 `.glb` 文件：
- **glTF Viewer**: https://gltf-viewer.donmccurdy.com/
- **Sketchfab**: 上传后可以预览

---

## 🔧 高级配置选项

### 如果模型颜色仍然不对

#### 1. 增加环境光
```html
<model-viewer 
    environment-image="neutral"
    exposure="1.5"
    ...>
</model-viewer>
```

#### 2. 使用自定义环境贴图
下载 HDR 环境贴图，放在项目中：
```html
<model-viewer 
    skybox-image="path/to/environment.hdr"
    ...>
</model-viewer>
```

#### 3. 调整色调映射
```html
<model-viewer 
    tone-mapping="auto"  <!-- 或 "commerce" 或 "neutral" -->
    ...>
</model-viewer>
```

---

## 📱 功能特性

已实现的交互功能：
- ✅ **360° 旋转** - 鼠标拖拽
- ✅ **缩放** - 鼠标滚轮
- ✅ **平移** - 右键拖拽（或触摸屏双指）
- ✅ **自动旋转** - 可开关
- ✅ **重置视角** - 一键恢复
- ✅ **AR 支持** - 移动设备上可查看 AR

---

## 🐛 常见问题

### Q: 模型显示为纯黑色
**A:** 
1. 检查 Rhino 导出时是否勾选了 "Export Materials"
2. 尝试增加 `exposure` 值（如 `2.0`）
3. 确保模型在 Rhino 中有材质分配

### Q: 模型显示为白色/灰色
**A:** 
1. 检查材质颜色设置
2. 调整 `exposure` 值（降低到 `0.8`）
3. 检查环境光设置

### Q: 模型加载很慢
**A:**
1. 优化模型：减少面数
2. 压缩纹理：使用较小的纹理贴图
3. 使用 `.glb` 格式（比 `.gltf` 更紧凑）

### Q: 模型位置不对
**A:**
1. 在 Rhino 中调整模型位置（居中、旋转）
2. 或使用 `camera-orbit` 属性调整初始视角

---

## 📝 文件结构

```
yifan profo/
├── Shoes/
│   └── 2/
│       ├── model.glb          ← 你的 3D 模型文件（需要创建）
│       ├── Product gray2.jpg
│       └── ...
└── project-modular-sneakers.html
```

---

## 🎨 推荐设置

### 最佳实践
1. **材质设置**：
   - 使用 PBR 材质（物理基础渲染）
   - 设置合适的粗糙度和金属度
   - 添加法线贴图增强细节

2. **模型优化**：
   - 面数控制在 10万-50万 之间
   - 纹理分辨率 2048x2048 或 1024x1024
   - 使用压缩纹理格式

3. **导出设置**：
   - 使用 `.glb` 格式（二进制，更小更快）
   - 包含所有材质和纹理
   - 检查文件大小（建议 < 50MB）

---

## ✅ 完成检查

完成以上步骤后，你的模型应该：
- ✅ 显示正确的颜色和材质
- ✅ 有适当的光照和阴影
- ✅ 可以流畅交互
- ✅ 在不同设备上正常显示

如果仍有问题，请检查浏览器控制台的错误信息。


